{
  "name": "kubeview-mcp",
  "version": "1.4.3",
  "description": "Read-only MCP server enabling code-driven AI analysis of Kubernetes clusters",
  "mcpName": "io.github.mikhae1/kubeview",
  "main": "dist/src/index.js",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js",
      "default": "./dist/src/index.js"
    }
  },
  "bin": {
    "kubeview-mcp": "./bin/kubeview-mcp.js",
    "kubeview-mcp-code-mode": "./bin/kubeview-mcp-code-mode.js"
  },
  "files": [
    "bin/",
    "dist/",
    "*.md"
  ],
  "scripts": {
    "build": "node scripts/build-version.js && tsc",
    "dev": "tsx --watch src/cli/cli.ts",
    "start": "tsc && node bin/kubeview-mcp.js",
    "setup": "node bin/setup.js",
    "code-mode": "node bin/kubeview-mcp-code-mode.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "lint": "eslint . --ext .ts",
    "format": "prettier --write \"src/**/*.ts\" \"tests/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "prepare": "husky",
    "command": "node src/cli/run-command.js",
    "release": "npm run release:check && npm run release:qa && npm run release:build && npm run release:tag",
    "release:check": "git diff --exit-code && git diff --cached --exit-code || (echo 'Error: Working directory not clean. Please commit or stash changes.' && exit 1)",
    "release:qa": "npm run lint && npm run typecheck && npm run test",
    "release:build": "npm run build",
    "release:tag": "git tag v$(node -p \"require('./package.json').version\") && echo 'Tagged version v'$(node -p \"require('./package.json').version\")",
    "publish": "npm publish && git push origin master --tags"
  },
  "keywords": [
    "kubernetes",
    "mcp",
    "debugging",
    "k8s",
    "ai-tools",
    "ai-agent",
    "code-mode-mcp"
  ],
  "author": "mikhae1",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@types/jest": "^30.0.0",
    "@types/node": "^24.1.0",
    "@typescript-eslint/eslint-plugin": "^8.38.0",
    "@typescript-eslint/parser": "^8.38.0",
    "eslint": "^9.32.0",
    "eslint-config-prettier": "^10.1.5",
    "eslint-plugin-prettier": "^5.4.0",
    "husky": "^9.1.7",
    "jest": "^30.0.0",
    "lint-staged": "^16.1.0",
    "prettier": "^3.5.3",
    "ts-jest": "^29.4.5",
    "tsx": "^4.19.4"
  },
  "dependencies": {
    "@kubernetes/client-node": "^1.3.0",
    "@modelcontextprotocol/sdk": "^1.17.1",
    "isolated-vm": "^6.0.2",
    "typescript": "^5.8.3",
    "winston": "^3.17.0",
    "zod": "^3.25.36"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "overrides": {
    "test-exclude": "^7.0.1"
  }
}
